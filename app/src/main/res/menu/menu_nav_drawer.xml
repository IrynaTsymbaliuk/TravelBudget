<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group
        android:id="@+id/accounts_for_authorized_users"
        android:checkableBehavior="single">

        <item
            android:id="@+id/nav_sign_out"
            android:icon="@drawable/ic_person_outline_grey_24dp"
            android:title="@string/sign_out"/>

        <item
            android:id="@+id/nav_change_account"
            android:icon="@drawable/ic_person_add_grey_24dp"
            android:title="@string/change_account" />

    </group>

    <group
        android:id="@+id/accounts_for_unauthorized_users"
        android:checkableBehavior="single">

        <item
            android:id="@+id/nav_sign_in"
            android:icon="@drawable/ic_person_outline_grey_24dp"
            android:title="@string/sign_in"/>

    </group>

    <group
        android:id="@+id/settings"
        android:checkableBehavior="single">

        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_settings_grey_24dp"
            android:title="@string/settings" />

    </group>

    <group
        android:id="@+id/cv"
        android:checkableBehavior="single">

        <item
            android:id="@+id/nav_feedback"
            android:title="@string/send_a_feedback"
            android:icon="@drawable/ic_mail_grey_24dp"/>

        <item
            android:id="@+id/nav_offer"
            android:title="@string/make_a_job_offer"
            android:icon="@drawable/ic_mail_grey_24dp"/>

    </group>

</menu>